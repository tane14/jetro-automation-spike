# Cursor Handoff — TASK-20260820-001

Created by: Claude Code (Architect/Reviewer)
Task ID: TASK-20260820-001
Contract ID: CONTRACT-20260820-001
Risk Tier: T0
Status: READY_FOR_CURSOR

## Contract validation

Claude Code reviewed `control-plane/tasks/TASK-20260820-001.md` against `control-plane/CONTROL_PLANE_SPEC.md` and the approval record at `control-plane/approvals/TASK-20260820-001.md`.

- Contract is internally consistent; no contradictions found.
- `PRE_EXECUTION_APPROVAL` artifact is present and matches the required shape from the Control Plane spec (Task ID, Contract ID, Risk Tier, Decision, Approver Identity, Timestamp, Rationale) — Decision: APPROVED.
- Scope is laboratory-only; no protected operation is required by this contract.

## Objective

Implement a small JavaScript utility and its tests, using only Node built-ins, exactly as specified in `control-plane/tasks/TASK-20260820-001.md`.

## Required implementation

- Create `src/control-plane-task.js`.
- Export exactly one function named `greet(name)`.
- The function must return exactly `Hello, ${name}!`.

## Required tests

- Create `src/control-plane-task.test.js`.
- Cover `greet("World")` → `"Hello, World!"`.
- Cover `greet("")` → `"Hello, !"`.
- Run with `node --test src/control-plane-task.test.js`.
- Do not add dependencies.
- Do not modify existing files outside the two new `src/` files and the required Control Plane evidence/handoff artifacts.

## Constraints

- Laboratory only. Do not access VPS, production, JETRO/IBE, databases, or live services.
- Do not modify `main`.
- Do not merge or deploy.
- Publish the implementation on a dedicated Cursor branch.
- Record execution evidence under `control-plane/evidence/TASK-20260820-001/` using the Evidence Contract fields defined in the Control Plane spec (task ID, contract ID, risk tier, current state, agent, branch, commit, files changed, validation command and result, review result, risks/blockers, approval record reference).

## Next state

Per `control-plane/tasks/TASK-20260820-001.md`, required progression after this handoff is: EXECUTING → VALIDATING → REVIEW_READY. Cursor owns the EXECUTING and VALIDATING transitions; REVIEWED and COMPLETION_APPROVAL remain with the Reviewer and Human Approval Authority respectively.
