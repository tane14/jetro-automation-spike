# TASK-20260820-001

Status: CONTRACT_READY
Risk Tier: T0
Environment: LABORATORY ONLY
Contract ID: CONTRACT-20260820-001

## Objective
Validate the v0.1 Control Plane lifecycle with one minimal, non-production task while exercising contract validation, implementation handoff, execution, evidence, review, and explicit completion approval.

## Task
Create a small JavaScript utility and tests, using only Node built-ins.

## Required implementation
- Create `src/control-plane-task.js`.
- Export exactly one function named `greet(name)`.
- Return exactly `Hello, ${name}!`.

## Required tests
- Create `src/control-plane-task.test.js`.
- Cover `greet("World")` → `Hello, World!`.
- Cover `greet("")` → `Hello, !`.
- Use `node --test src/control-plane-task.test.js`.
- Do not add dependencies.
- Do not modify existing files outside the two new `src/` files and the required Control Plane evidence/handoff artifacts.

## Agent assignments
- ChatGPT: Orchestrator. Owns task state and gate progression; must not infer human approval.
- Claude Code: Architect/Reviewer. Validate this contract and produce the Cursor handoff; do not implement repository changes.
- Cursor: Executor. Implement exactly this contract on a dedicated branch, run the required test, and publish evidence.
- Human: Approval Authority. Explicitly approves pre-execution and completion transitions.

## State requirements
Initial state: CONTRACT_READY
Required progression: PRE_EXECUTION_APPROVAL → EXECUTING → VALIDATING → REVIEW_READY → REVIEWED → COMPLETION_APPROVAL → APPROVED → COMPLETED

Exceptions must follow the Control Plane specification. No agent may silently skip a required state.

## Approval rule
No conversational statement from an agent constitutes human approval. A valid approval must be recorded in `control-plane/approvals/` using the required approval artifact shape from the Control Plane specification.

## Evidence rule
Execution evidence must be recorded under `control-plane/evidence/TASK-20260820-001/` using the required Evidence Contract fields.

## Safety
- Laboratory only.
- Do not access VPS, production, JETRO/IBE, databases, or live services.
- Do not modify `main`.
- Do not merge or deploy.
- If any instruction is contradictory, incomplete in a way that prevents safe execution, or violates the boundary: STOP and mark BLOCKED.

## Current gate
CONTRACT_READY

## Next action
Human approval is required before execution. Until a valid `PRE_EXECUTION_APPROVAL` artifact exists, agents must not implement the task.
